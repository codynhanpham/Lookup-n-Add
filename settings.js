const settings = {
    lookupTablePath: "", // Path to the lookup table, example, "C:/Downloads/lookup-table.csv"
    lookupKey: "", // The column to lookup, example, "ID"
    lookupValues: "", // The columns to add, comma separated, example, "Name,Function,Position"

    inputDataPath: "", // Path to the data, example, "C:/Downloads/data.csv"
    dataKey: "", // The column to lookup, example, "sampleID"
    dataValues: "", // The columns to add, comma separated, example, "sampleName,sampleFunction,samplePosition"

    checkedDuplicateKeys: false,
}

export default settings;